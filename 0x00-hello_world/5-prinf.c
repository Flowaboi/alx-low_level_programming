#include <stdio>
/**
 * main -a program that prin a line using printf function
 * Return: 0 (success)
 */
int main(void)
{
printf("with proper garmmer, but the outcome is a piece of art \n")
	return (0);
}
